Create Obsidian flashcards using the Flashcards plugin format.

Rules:

- Add the tag **#flashcards/Japanese_Basics** at the top.
- Use **single-line cards** in the format `Question::Answer`.
- The **question must contain ONLY Japanese text + romaji in parentheses**.
- The **answer must contain English and Portuguese separated by " / "**.
- One card per line.
- Do not add explanations or extra text.
- Output only the final note ready to paste.

Words to convert: